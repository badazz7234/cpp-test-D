#include <iostream>
using namespace std;

int main(){

    string boolean;

    cout << "Are you BLACK? (YES or No)";
    cin >> boolean;

    if(boolean == "yes" || boolean == "YES" || boolean == "Yes" || boolean == "yEs" || boolean == "yeS" || boolean == "YEs" || boolean == "YeS" || boolean == "yES"){
        cout << "GO TO JAIL NIGGA";
    }

    else if(boolean == "no" || boolean == "No" || boolean == "NO" || boolean == "nO"){
        cout << "Enjoy your vacation gentleman :D ";
    }

    else{
        cout << "ENTER A VALUE BITCH ASS NIGGA";
    }
    
    /*cout << "Are you black? (Yes or No):  ";
    ;*/
    
    
    return 0;
}